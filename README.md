# Animated-User-login-in-HTML
This code does not work as HTML is incapable of processing inputs from the user. 
It doesn't work.
It's like a dead button.
In this code, we use multiple input-type commands to get input from the user. 
